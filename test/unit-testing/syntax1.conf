# this is a comment

server {
    location /hello {
        default_type text/html;
        content_by_lua_block {
            ngx.say("HelloWorld!")
            -- lua comment
        }
        client_max_body_size 1m;
    }
    location / {
        content_by_lua_block {
--[[}
    }
    --]]}

}
